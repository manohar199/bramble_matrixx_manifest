<?xml version="1.0" encoding="UTF-8"?>
<manifest>
  <!-- Pixel 4a 5G (bramble) Device Trees for LineageOS 23.2 -->
  
  <!-- Device Tree (includes sepolicy inside) -->
  <project path="device/google/bramble" 
           name="LineageOS/android_device_google_bramble" 
           remote="github" 
           revision="lineageos-23.2"
           groups="device,bramble" />
  
  <!-- Common Redbull Platform (includes sepolicy inside) -->
  <project path="device/google/redbull" 
           name="LineageOS/android_device_google_redbull" 
           remote="github" 
           revision="lineage-23.2"
           groups="device,redbull" />
  
  <!-- Kernel -->
  <project path="kernel/google/redbull" 
           name="LineagOS/android_kernel_google_redbull" 
           remote="github" 
           revision="lineageos-23.2"
           groups="device,bramble,redfin"
           clone-depth="1" />
  
  <!-- Vendor Blobs (TheMuppets) -->
  <project path="vendor/google/bramble" 
           name="LineageOS/proprietary_vendor_google_bramble" 
           remote="github" 
           revision="lineage-23.2"
           clone-depth="1" />
</manifest>
